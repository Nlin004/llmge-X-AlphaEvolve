

```text
You are repairing a partially broken machine-generated Python candidate.

Repair priorities:
1. make the file syntactically valid Python
2. preserve the candidate's intended structure when possible
3. keep the file executable as-is
4. keep the `main()` entry point
5. keep writing `design.v` under `--save_dir`

Current code:
```python
{}
