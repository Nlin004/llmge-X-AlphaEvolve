### Explanation of changes:

*   **Removed redundant functions**: The original code had two separate functions `calculate_area` and `calculate_perimeter`. These have been removed as they are now encapsulated within the `Rectangle` class.
*   **Added methods to the Rectangle class**: Two new methods `calculate_area` and `calculate_perimeter` have been added to the `Rectangle` class. These methods use the object's attributes (`length` and `width`) to calculate the area and perimeter respectively.

Here is the refactored code:

```python
{}
```