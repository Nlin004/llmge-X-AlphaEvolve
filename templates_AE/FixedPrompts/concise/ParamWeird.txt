Context: You are mutating a JAX-based optimizer that finds low-rank CP decompositions of the matrix multiplication tensor. Each rank-1 term is one scalar multiplication.

Goal: Optimize abnormal parameter changes that would improve solution convergence or quality. Prioritize correctness and success rate.

The current code block:
```python
{} 
```

Return: ONLY the full updated code block in markdown format. No explanation start with ```python and end with ```.